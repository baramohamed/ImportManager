<UserControl x:Class="ImportManager.View.PaiementControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ImportManager.View"
             xmlns:MaterialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             MinWidth="600"
             d:DesignHeight="700" d:DesignWidth="800">
    <StackPanel>
        <DatePicker MaterialDesign:HintAssist.Hint="Date"/>
        <ComboBox MaterialDesign:HintAssist.Hint="Source"/>
        <ComboBox MaterialDesign:HintAssist.Hint="Destinataire"/>
        <DockPanel>
            <Button Content="{StaticResource SettingsIcon}"
                    DockPanel.Dock="Right"
                    Style="{StaticResource IconButton}"/>
            <ComboBox MaterialDesign:HintAssist.Hint="Mode Règlement"/>
        </DockPanel>
        <TextBox MaterialDesign:HintAssist.Hint="Montant"/>
    </StackPanel>
</UserControl>
